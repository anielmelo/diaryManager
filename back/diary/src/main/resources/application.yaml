spring:
  application:
    name: diary
  datasource:
    url: jdbc:mysql://localhost:5555/diary
    username: adm
    password: adm
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

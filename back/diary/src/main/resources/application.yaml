spring:
  application:
    name: diary
  datasource:
    url: jdbc:mysql://localhost:5005/diary
    username: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
